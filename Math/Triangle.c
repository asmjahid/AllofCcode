/*Programe to calculate triangle*/
#include<stdio.h>
main()
{
float base,height,area;
printf("base: ");
scanf("%f", &base);
printf("height: ");
scanf("%f", &height);
area=(base*height)/2.;
printf("area: %f", area);
}
